package org.planit.graph;

import org.planit.utils.id.ExternalIdable;

public class GraphEntityImpl<E extends ExternalIdable> {
  
  TODO -> add options to override id for other impl classes -> GraphFactoryImpl etc.

}
