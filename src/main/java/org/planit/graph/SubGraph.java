package org.planit.graph;

/**
 * 
 * A sub graph contains a subset of the (implicit) full graph. The active subset of the graph is tracked by explicitly registering edge (segments) which in turn activate its a and
 * b nodes on the sub-graph.
 * 
 * 
 * 
 * @author markr
 *
 */
public class SubGraph {

  public SubGraph() {

  }

}
